import { ResponseSubmittedTimePipe } from './response-submitted-time.pipe';

describe('ResponseSubmittedTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ResponseSubmittedTimePipe();
    expect(pipe).toBeTruthy();
  });
});
