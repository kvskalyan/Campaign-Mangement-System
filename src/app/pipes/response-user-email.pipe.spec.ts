import { ResponseUserEmailPipe } from './response-user-email.pipe';

describe('ResponseUserEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new ResponseUserEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
