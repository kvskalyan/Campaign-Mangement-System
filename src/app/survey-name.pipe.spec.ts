import { SurveyNamePipe } from './survey-name.pipe';

describe('SurveyNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SurveyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
